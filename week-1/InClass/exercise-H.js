const myName = "Juan"
const myLastName = " Garcia"
const completeName = myName + myLastName

console.log(completeName);

const barcelonaGoals = 4
const napoliGoals = 2
const totalGoals = barcelonaGoals + napoliGoals
console.log(totalGoals)

//con esta funcion vamos a sumar el total de los goles entre el barcelona y el napoli y al utilizar "return", deberiamos obtener un string
function add(barcelonaGoals, napoliGoals) {
return barcelonaGoals + napoliGoals;
}
